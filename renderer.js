// This file is required by the index.html file and will
// be executed in the renderer process for that window.
// All of the Node.js APIs are available in this process.

/*
const BrowserWindow = require('electron').remote.BrowserWindow
const path = require('path')
const url = require('url')

const ventanaAnimacion = document.getElementById('animacion_2d')

ventanaAnimacion.addEventListener('click', function (event) {
   let win = new BrowserWindow({ width: 500, height: 500 })
   win.loadURL(url.format({
     pathname: path.join(__dirname, 'views/animacion2d.html'),
     protocol: 'file:',
     slashes: true
   }))

   win.show()

   win.on('close', function () { win = null })
 })
*/
